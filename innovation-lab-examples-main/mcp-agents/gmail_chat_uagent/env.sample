# Example environment configuration for Gmail Chat Agent
# Copy this file to `.env` (which is git-ignored) and fill in real credentials before running.

# --- OpenAI API ---
OPENAI_API_KEY="sk-..."
OPENAI_MODEL="o3-mini"

# --- Gmail Chat Agent ---
GMAIL_CHAT_AGENT_PORT=8088
GMAIL_CHAT_AGENT_NAME="gmail_chat_agent"
GMAIL_CHAT_AGENT_SEED="any random string for uniquely idenitfying your agent"

# Directory where temporary OAuth token files are stored
GMAIL_TOKENS_DIR=".tokens"

# --- Gmail FastMCP Server ---
GMAIL_MCP_PORT=8081
# When the Gmail MCP is exposed via ngrok or similar, place the full SSE URL here
GMAIL_MCP_URL="https://your-ngrok-subdomain.ngrok.app/sse/"

# Enable verbose Gmail logging (true/false)
VERBOSE_GMAIL_LOGS=true

# --- Logging paths (optional overrides) ---
GMAIL_LOG="gmail_debug.log"
GMAIL_CHAT_LOG="gmail_chat_debug.log"
GMAIL_CHAT_IO_LOG="gmail_chat_io.log"

# --- Agentverse integration (optional) ---
AGENTVERSE_API_KEY=""
AGENTVERSE_URL="https://agentverse.ai"

# --- Misc ---
MAX_GMAIL_CHAT_HISTORY=10 
